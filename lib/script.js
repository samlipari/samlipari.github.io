/* const softwareEng = document.querySelectorAll('#software-eng path');

for (let index = 0; index < softwareEng.length; index++) {
  console.log(`letter ${index} is ${softwareEng[index].getTotalLength()}`);
} */